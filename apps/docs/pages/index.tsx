import { Button, Header } from "ui";

export default function Docs() {
  return (
    <div>
      <h1>Docs</h1>
      <Header
        onLogin={() => {}}
        onLogout={() => {}}
        onCreateAccount={() => {}}
      />
      <Button label="Wooo" />
    </div>
  );
}
